<div class="form-group {{if field.hasErrors "has-error"}}">
  <label class="control-label col-sm-2" for={{uid}}>
    {{field.label}}
  </label>
  <div class="col-sm-10">
    {{fast-select
      class="form-control"
      valuePath="id"
      id=field.uid
      items=items
      value=field.value}}
  </div>
  {{#if field.errors}}
    <p class="col-sm-offset-2 help-block">{{field.errors.firstObject.detail}}</p>
  {{/if}}
</div>

