{{#if template}}
  {{yield this}}
{{else if items}}
{{!-- XXX not a huge fan of this, but select boxes require an extra paramenter that is fetched by the outer component macro --}}
  {{component fieldComponentName field=this items=items}}
{{else}}
  {{component fieldComponentName field=this}}
{{/if}}
