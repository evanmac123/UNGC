{{#each nodes as |node|}}
  {{tree/x-dropzone node=node position="before"}}
  {{tree/x-node node=node}}
{{/each}}

{{#if nodes.lastObject}}
  {{tree/x-dropzone node=nodes.lastObject position="after"}}
{{/if}}
