-@title = "#{@communication_on_progress.organization_name} &ndash; #{@communication_on_progress.title}"
%h1 Communication on Progress

#main_content
  %dl
    %dt Participant:
    %dd
      %ul
        %li= link_to @communication_on_progress.organization_name, participant_link(@communication_on_progress.organization, params[:navigation])

    - if @communication_on_progress.cop_answers.any? || @communication_on_progress.is_grace_letter?
      = render :partial => '/shared/cops/show_new_style.html.haml', :locals => { :communication_on_progress => @communication_on_progress } 
    - else
      = render :partial => '/shared/cops/show_legacy_style.html.haml', :locals => { :communication_on_progress => @communication_on_progress } 

  .note
    %p{:style => 'margin-top: 2em; font-size: 10px; line-height: 1.5em;'} Note: Responsibility for the content of participants' Communication on Progress and any other public communication related to the Global Compact principles and their implementation lies with participants themselves and not with the UN Global Compact Office.