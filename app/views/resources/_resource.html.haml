%li.resource
  %a{href: resource_path(resource), title: resource.title}
    .image
      = image_tag resource.image_url if resource.image_url
    %h5.title
      = truncate(resource.title, length: 46, separator: ' ')

