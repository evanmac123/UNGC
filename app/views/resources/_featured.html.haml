%h1 Featured

.display_shelf-section.ungc

  %ul.display_shelf
    - @featured.featured.each do |f|
      = render f

.display_shelf-section.ungc
  .display_shelf-header
    = link_to_all_resources 'global_compact'
    %h3 Global Compact

  %ul.display_shelf
    - @featured.global_compact.each do |f|
      = render f

.display_shelf-section.human_rights
  .display_shelf-header
    = link_to_all_resources 'human_rights'
    %h3 Human Rights

  %ul.display_shelf
    - @featured.human_rights.each do |f|
      = render f
