.resource-toggle-views
  %button.browse Browse
  %button.search Search

#resources_featured
  = render :partial => 'featured'

#resources_search{:class => "#{ @results ? 'hidden' : ''}" }
  = render :partial => 'search_form'
