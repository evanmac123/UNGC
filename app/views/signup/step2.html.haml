- @css = ['forms', 'application_form']
%h1== #{signup_type} Organization Application
%h2 Primary Contact Point
%p This person will be the main point of contact between your organization and the Global Compact Office.
#main_content
  - form_for(@contact, url: organization_step3_path) do |form|
    = form.error_messages
    = render :partial => 'contact_form', :locals => {:form => form, :account_info => true}
    = form.submit 'Next', :class => 'button'
