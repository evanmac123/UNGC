<%= render partial: 'components/meta_tags' %>
<%= render partial: 'components/hero' %>
<section class='main-content-section main-content-section-narrow'>
  <div class='wrapper'>
    <header class='main-content-header'>
      <h1><%= signup_type -%> Organization Application</h1>
    </header>

    <%= form_for(@signup.primary_contact, url: organization_step3_path, html: {class: 'form-classic signup'}) do |form| %>

      <%= form.error_messages header_message: false %>
      <%= render 'flash_error_messages' %>

      <h2>Primary Contact Point</h2>

      <p>The main point of contact between your organization and the Global Compact Office. After your application is reviewed and accepted, this person will receive regular email including the UN Global Compact Monthly Bulletin, Communication on Progress deadlines (for business participants) as well as updates on news and events.</p>

      <%= render partial: 'contact_form', locals: { form: form, account_info: true, contact_type: 'Primary Contact Point', financial_contact: false } %>

      <%= form.submit 'Next', class: 'button' %>

    <%- end -%>
  </div>
</section>
