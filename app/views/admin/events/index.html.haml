%h3 Events

- if paged_events.any?
  %table
    %thead
      %tr
        %th Title
        %th Dates
        %th Location
        %th &nbsp;
    %tbody
      - for event in paged_events do
        %tr
          %td= event.title
          %td= date_range event
          %td= event.full_location
          %td
            = link_to 'Edit', edit_admin_event_path(event)
            = link_to 'Delete', admin_event_path(event), :class => 'link_to_destroy', :confirmation => 'Are you sure you want to delete this event?'
          
= link_to 'Create an event', new_admin_event_path
