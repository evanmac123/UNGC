%h2 New role

- form_for @role, builder: DefaultFormBuilder, url: admin_roles_path do |form|
  = render :partial => form
  %ul{:class => "tools inline" :style => 'float: right;'}
    %li= link_to 'Cancel', admin_roles_path, :class => 'cancel_large'
    %li= form.submit 'Create', :class => 'button'