%h2 New COP question

%p Question attributes (checklist items) can be added after the question is created.

- form_for @cop_question, builder: DefaultFormBuilder, url: admin_cop_questions_path do |form|
  = render :partial => form
  %ul{:class => "tools inline" :style => 'float: right;'}
    %li= link_to 'Cancel', admin_cop_questions_path, :class => 'cancel_large'
    %li= form.submit 'Create', :class => 'button'
