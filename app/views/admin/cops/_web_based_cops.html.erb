<% cop.cop_links.each_with_index do |link, index| %>
  <%= render partial: 'cop_link', locals: { cop_link: link, record: index } %>
<% end %>
<%= render partial: 'cop_link', locals: { cop_link: cop.new_link, record: cop.cop_links.count } %>

          
