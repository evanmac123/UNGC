%h2 New COP

- form_for([@organization, @communication_on_progress], :html => { :multipart => true }) do |form|
  = render :partial => form  
  %p
    = form.submit 'Save'

= link_to 'Back', dashboard_path