%h2== Search results "#{@searched_for}"

%div
  %p
    Number of matching contacts:
    %strong= @results.total_entries

%table.dashboard_table
  %tr
    %th ID
    %th Name
    %th Organization
    %th Email
    %th Actions

  - for contact in @results
    %tr
      %td= contact.id
      %td= contact.name
      %td= truncate(contact.organization.name)
      %td= contact.email
      %td
        %ul.actions
          %li 
            = link_to 'Edit', edit_admin_organization_contact_path(id: contact.id, organization_id: contact.organization_id), :class => "edit", :title => "Edit"
          %li 
            = link_to 'Destroy', admin_organization_contact_path(id: contact.id, organization_id: contact.organization_id), :class => "delete", :title => "Delete", :confirm => 'Are you sure?', :method => :delete

= will_paginate @results, :anchor => 'paged_results'
