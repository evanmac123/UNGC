%h2== Search results "#{@searched_for}"

%div
  %p
    Number of matching contacts:
    %strong= @results.total_entries

%table.dashboard_table
  %tr
    %th ID
    %th Name
    %th Organization
    %th Email
    %th.center CEO
    %th.center Contact Point
    %th Actions

  - for contact in @results
    %tr
      %td= contact.id
      %td= contact.name
      %td{:title => contact.organization_name}= truncate contact.organization_name, :length => 40
      %td= contact.email
      %td.center= image_tag('/images/checked.png') if contact.is?(Role.ceo)
      %td.center= image_tag('/images/checked.png') if contact.is?(Role.contact_point)
      %td
        %ul.actions
          %li 
            = link_to 'Edit', edit_contact_path(contact), :class => "edit", :title => "Edit"
          %li 
            = link_to 'Destroy', contact_path(contact), :class => "delete", :title => "Delete", :confirm => 'Are you sure?', :method => :delete

= will_paginate @results, :anchor => 'paged_results'
