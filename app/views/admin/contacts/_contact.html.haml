%li
  = contact.name
  
  = link_to 'edit', edit_admin_organization_contact_path(contact.organization.id, contact)
  - unless contact == current_user
    = link_to 'delete', admin_organization_contact_path(contact.organization.id, contact), :confirm => 'Are you sure?', :method => :delete
  