%h3= link_to 'Pages', admin_pages_path

%h3= link_to 'Organizations', admin_organizations_path

%h3= link_to 'Events', admin_events_path

%h3= link_to 'News headlines', admin_headlines_path

%h3= link_to 'Reports', reports_path

%h3 Pending Organizations
- if @pending_organizations.any?
  %ul
    = render :partial => 'admin/organizations/organization', :collection => @pending_organizations
- else
  no work for you

%h3 Organizations under review
- if @in_review_organizations.any?
  %ul
    = render :partial => 'admin/organizations/organization', :collection => @in_review_organizations
- else
  no work for you

%h3 Pending Logo Requests
- if @pending_logo_requests.any?
  %ul
    = render :partial => 'admin/logo_requests/logo_request', :collection => @pending_logo_requests
- else
  no work for you

%h3 Logo Requests that need attention
- if @unreplied_logo_requests.any?
  %ul
    = render :partial => 'admin/logo_requests/logo_request', :collection => @unreplied_logo_requests
- else
  no work for you

%h3 Pending Case Stories
- if @pending_case_stories.any?
  %ul
    = render :partial => 'admin/case_stories/case_story', :collection => @pending_case_stories
- else
  no work for you

%h3 Pending COPs
- if @pending_cops.any?
  %ul
    = render :partial => @pending_cops
- else
  no work for you
