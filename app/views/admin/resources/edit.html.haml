%h2
  Edit Resource
  =truncate(@resource_form.title, :length => 60)

= form_for @resource_form, builder: DefaultFormBuilder, :url => admin_resource_path(@resource_form.resource), method:'put', :html => {:multipart => true} do |form|
  = render :partial => form

  %ul{:class => "tools inline", :style => 'float: right;'}
    %li= link_to 'Cancel', admin_resource_path(@resource_form.resource), :class => 'cancel_large'
    %li= form.submit 'Create', :class => 'button'
