%h2 New platform

%ul.tools.inline
  %li= render :partial => 'admin/action_platform/platforms/back_to_index'

= form_for @platform, builder: DefaultFormBuilder, :url => admin_action_platform_platforms_path do |form|
  = render :partial => form
  = form.submit 'Create', :class => 'button'
