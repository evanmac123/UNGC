%h2 Logo Request

- form_for(:logo_request, builder: DefaultFormBuilder, url: admin_organization_logo_requests_path(@organization.id), :html => { :multipart => true }) do |form|
  = render :partial => form
  = form.submit 'Submit', :class => 'button'
