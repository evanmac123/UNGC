%h3= header_string

%ul{:class => "tools inline"}
  %li= link_to 'Upload file', new_attachment_path, :class => "new_large"
  %li= link_to cancel_string, cancel_path, :class => 'go_back_large'

%table.dashboard_table
  %tr
    %th File name
    %th Size
    %th Actions
  - attachments.each do |a|
    = render :partial => '/admin/attachments/attachment', :object => a

