%h2 Edit Meeting

- form_for @submodel, builder: DefaultFormBuilder, :url => submodel_path(@submodel), :html => {:multipart => true} do |form|
  = render :partial => form
  %ul{:class => "tools inline" :style => 'float: right;'}
    %li= link_to 'Cancel', return_url, :class => 'cancel_large'
    %li= form.submit 'Save changes', :class => 'button'
