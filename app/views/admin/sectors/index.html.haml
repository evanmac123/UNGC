%h2 Listing sectors
= link_to 'Create a sector', new_admin_sector_path, :class => "new", :id => "new_sector"

%table.dashboard_table#sectors_table
  %tr
    %th.last_updated Last updated on
    %th.title Name
    %th.status Parent
    %th.actions Actions
  = render @sectors
