%h2 Edit Memorandum of Understanding

- form_for @submodel, builder: DefaultFormBuilder, :url => admin_local_network_mou_path(@local_network, @submodel), :html => {:multipart => true} do |form|
  = render :partial => form
  = form.submit 'Update', :class => 'button'

