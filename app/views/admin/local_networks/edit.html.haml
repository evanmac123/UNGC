%h2 Editing Local Network

- form_for @local_network, builder: DefaultFormBuilder, url: admin_local_network_path(@local_network) do |form|
  = render :partial => form
  = form.submit 'Save changes', :class => 'button'
