<%# Meta Tags %>
<%-
title ||= @page.meta_title
-%>
<% provide :title, HTMLEntities.new.decode(title).html_safe %>
<% provide :description, @page.meta_description.try(:html_safe) %>
<% provide :keywords, @page.try(:meta_keywords) %>
