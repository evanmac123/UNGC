<%= render partial: 'redesign/components/meta_tags' %>
<%# Body %>
<%= render partial: 'redesign/components/hero' %>

<section class='main-content-section'>
  <%= content_tag(:header, content_tag(:h1, @page.title), {class: 'main-content-header'}) %>

  <h3>Main Page</h3>
  <section class='main-page'>
    <div class='content'>
      <%= @page.description.try(:html_safe) %>
      <%- # XXX sponsor list will be in the description HTML for the main page %>

      <div class='sidebar'>
        <%= @page.date.string_with_time %>
        <%= @page.location %>

        <%= render 'redesign/components/widget_contact', contact: @page.contact if @page.contact%>

        <%= render 'redesign/components/widget_calls_to_action', calls_to_action: @page.calls_to_action if @page.calls_to_action.any? %>
      </div>

    </div>
  </section>

  <h3>Overview</h3>
  <section class='overview'>
    <%= @page.overview_description.try(:html_safe) %>
  </section>

  <h3>Sponsors</h3>
  <section class='sponsors'>
    <%- @page.sponsors.each do |s| -%>
      <%= s.name %>
      <%= s.website_url %>
      <%= image_tag s.logo_url %>
    <%- end -%>
  </section>

</section>

<%- #XXX @jorge no content boxes even if you seen then !!!%>
