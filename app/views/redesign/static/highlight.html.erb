<%# Meta Tags %>
<% provide :title, @page.meta_title %>
<% provide :description, @page.meta_description %>

<%# Body %>
<%= render partial: 'redesign/components/hero' %>

<%- @page.main_content_sections.each do |section| -%>
  <%= render layout: 'redesign/components/main_content_section', locals: {content: section} do %>
    <%= render layout: 'redesign/components/sidebar' do %>
      <%= render('redesign/components/widget_image', image: section[:image]) if section[:image] %>
    <% end %>
  <% end %>
<%- end -%>

<%= render layout: 'redesign/components/content_blocks', locals: {title: @page.resources.empty? ? '' : 'From our Library'} do %>
  <%- @page.resources.each do |resource| -%>
    <%= render 'redesign/components/content_block', type: 'resource', title: resource.title, url: redesign_library_resource_path(resource), thumbnail: "//unglobalcompact.org#{resource.cover_image(:show, retina: true)}", tag: t(resource.content_type) %>
  <%- end -%>
<% end %>
