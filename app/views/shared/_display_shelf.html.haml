%ul.display_shelf.embedded-shelf
  - find_resources(ids).each do |r|
    = render partial: 'resources/resource', object: r
- if local_assigns[:category]
  = link_to_all_resources category
  %br/
  %br/
  %br/

