<div class="field" id="select-invoice-date">
  Please select your company’s preferred invoice date:
  <% options.each do |text, date| %>
    <label>
      <%= radio_button_tag(:preset_invoice_dates, date) %>
      <%= text %>
    </label>
  <% end %>
  <%# This is a hack for setting the max date range,  a policy is needed on how far to allow an organization to delay 
   their invoice date%>
  <%= form.date_field(:invoice_date, min: Date.current, max: Date.new(2019,1,31)) %>
</div>
